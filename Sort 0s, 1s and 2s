def leaders(self, arr):
        res=[]
        right_max = arr[-1]
        res.append(right_max)
        for i in range(len(arr)-2,-1,-1):
            val=arr[i]
            if val>=right_max:
                res.append(val)
                right_max=val
        return res[::-1]
